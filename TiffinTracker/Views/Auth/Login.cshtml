@{
    ViewData["Title"] = "Login";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-5">
            <div class="card shadow">
                <div class="card-body">
                    <h3 class="text-center text-primary mb-4">Login</h3>
                    @Html.Partial("_LoginForm") <!-- Partial inserted -->
                </div>
            </div>
        </div>
    </div>
</div>
